# Project Configuration File
# Use with config::get() to load environment-specific settings
# ============================================================

default:
  project_name: "Iowa Cities Data Analysis"
  author: "Your Name"
  
  # Data paths
  data:
    raw_dir: "data/raw"
    processed_dir: "data/processed"
    external_dir: "data/external"
  
  # Cache settings
  cache:
    enabled: true
    directory: "data/cache"
    timeout_hours: 24
    
  # Output settings
  outputs:
    figures_dir: "outputs/figures"
    tables_dir: "outputs/tables"
    reports_dir: "outputs/reports"
    logs_dir: "outputs/logs"
    figure_width: 10
    figure_height: 6
    figure_dpi: 300
  
  # Analysis parameters
  analysis:
    confidence_level: 0.95
    seed: 42
    missing_threshold: 0.10  # Flag columns with >10% missing

development:
  inherits: default
  analysis:
    verbose: TRUE
    save_intermediates: TRUE
  cache:
    timeout_hours: 1  # Shorter cache for development

production:
  inherits: default
  analysis:
    verbose: FALSE
    save_intermediates: FALSE
  outputs:
    figure_dpi: 600
  cache:
    timeout_hours: 48  # Longer cache for production
