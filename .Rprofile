# R Profile - Auto-loaded when opening this project
# ================================================

# Set CRAN mirror
options(repos = c(CRAN = "https://cloud.r-project.org"))

# Project-specific options
options(
  scipen = 999,                    # Avoid scientific notation
  stringsAsFactors = FALSE,        # Don't auto-convert strings to factors
  digits = 4,                      # Decimal places to display
  warn = 1,                        # Show warnings immediately
  warnPartialMatchDollar = TRUE,   # Warn on partial $ matches
  warnPartialMatchArgs = TRUE      # Warn on partial argument matches
)

# Startup message
if (interactive()) {
  message("\n📊 R Data Analysis Workspace loaded!")
  message("   Run source('scripts/00_setup.R') to load packages")
  message("   Run source('example_workflow.R') for a demo\n")
}
